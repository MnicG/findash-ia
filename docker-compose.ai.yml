version: "3.9"

services:
  ai:
    build:
      context: ./findash-ia
    ports:
      - "8000:8000"
    env_file:
      - ./findash-ia/.env
    environment:
      - ALLOWED_ORIGINS=http://localhost:5173
    restart: unless-stopped